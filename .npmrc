# The project ships an eslint 9 / @typescript-eslint 7 combination whose peer
# ranges do not line up. npm 7+ treats that as a hard error and aborts the
# install (this is what makes Cloudflare Workers Builds fail on `npm install`).
# The conflict is dev-tooling only and does not affect the runtime bundle.
legacy-peer-deps=true
